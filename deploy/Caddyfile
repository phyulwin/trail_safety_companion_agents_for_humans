# Caddyfile - Automatic public certificates, HTTP redirects, and WebSocket proxying.
{$TRAIL_HOST} {
    encode gzip
    header Strict-Transport-Security "max-age=31536000"
    reverse_proxy frontend:8080
}
