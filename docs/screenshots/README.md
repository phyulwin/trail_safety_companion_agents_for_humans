# Screenshot placeholders

The application was visually inspected in the local browser; this directory reserves `dashboard.png`, `checkin.png`, `community.png`, and `retrace.png` for exported submission captures, and no nonexistent image is embedded as a completed screenshot.
